{{#partial "content"}}
    <div class="container">
        <div class="indexRow">
            <div class="col-md-12 mb-4 white-text text-center">
                <h3 class="text-warning text-uppercase font-weight-bold mb-0 pt-md-5 pt-5 animated wobble"><strong> Locations: </strong></h3>
                <div class="row">
                    <div class="col-sm-4 col-sm-offset-4">
                        <a href="/locations/new"><button type="button" class="btn btn-info">Add Location</button></a>
                        <h3>Locations:</h3>
                            {{#if locations}}
                                {{#each rangers}}
                                    <p class="text" id={{locations.id}}>{{locations.name}}</p><br>
                                {{/each}}
                                {{#if sightings}}
                                    <h1>Sightings</h1>
                                    <h3>Time</h3>
                                        {{#each sightings}}
                                            <li>{{sightings.timestamp}}</li>
                                        {{/each}}
                                    <h3>Animal</h3>
                                        {{#each animals}}
                                            <li >{{animals}}</li>
                                        {{/each}}
                                    <h3>Type</h3>
                                    <hr>
                                        {{#each type}}
                                            <li>{{type}}</li>
                                        {{/each}}
                                {{/if}}
                            {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>
{{/partial}}

{{> layout.hbs}}